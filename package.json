{
  "name": "rio-languages",
  "version": "2.0.0",
  "description": "Rio Languages - Ứng dụng học ngoại ngữ đa ngôn ngữ (Tiếng Anh & Tiếng Trung)",
  "private": true,
  "scripts": {
    "build": "echo 'Web assets are ready in root'",
    "cap:sync": "cap sync ios",
    "cap:open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
